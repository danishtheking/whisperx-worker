# ---------- PyTorch CUDA 12.1 wheels ----------
# Use --index-url (not --extra-index-url) so torch comes from cu121, not PyPI
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple/
torch
torchaudio

# ---------- main libs ----------
whisperx
pyannote.audio
speechbrain>=1.0.0
ctranslate2

# ---------- speechbrain dependencies ----------
sentencepiece

# ---------- utilities ----------
pydub
numpy<2.0
scipy
librosa
huggingface-hub
runpod
cryptography
python-dotenv
