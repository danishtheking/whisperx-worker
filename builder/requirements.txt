# NOTE: torch and torchaudio are installed separately in the Dockerfile
# with --index-url cu121 to avoid CUDA version mismatch.

# ---------- main libs (pinned for consistent builds) ----------
whisperx>=3.3.0,<4.0
pyannote.audio>=3.3.0,<4.0
speechbrain>=1.0.0,<2.0
ctranslate2>=4.0.0,<5.0

# ---------- speechbrain dependencies ----------
sentencepiece

# ---------- utilities ----------
pydub
numpy<2.0
scipy
librosa
huggingface-hub
runpod
cryptography
python-dotenv
