# NOTE: torch and torchaudio are installed separately in the Dockerfile
# with --index-url cu121 to avoid CUDA version mismatch.

# ---------- main libs ----------
whisperx
pyannote.audio>=2.1,<3.2
speechbrain>=1.0.0
ctranslate2

# ---------- speechbrain dependencies ----------
sentencepiece

# ---------- utilities ----------
pydub
numpy<2.0
scipy
librosa
huggingface-hub
runpod
cryptography
python-dotenv
